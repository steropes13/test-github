#cours 19/03/26  : we will use GITHUB here 
Intelligence of git: only store the difference of the two files. 
branches : connexions that tells you how can move from one commit to another
.git : stores all the informations of the repository here 

git add : --all or "." or <name> 

branch : for each colleague or feature 
git branch devil 
lerning git branching : le jeu pour git
git branch : affiche les branches
git branch -d <nom> supprime une branche 
git checkout -b <nouvelle branche> permet de créer une branche et de s'y placer (au lieu de refaire un checkout après le branch)

commit : only on our machine 
issue (specifique à github) : write a description of the problem or the feature we want to implement. 
il est recommendé de se connecter via ssh 
generation d'une clé : 

création d'un dépot github sur le site 
connecter ce qui est fait sur notre machine via : 

git remote add origin <lien>
git remote get-url origin (donne le lien)